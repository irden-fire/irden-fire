'use strict';

describe('irdenPage.about_us module', function() {

  beforeEach(module('irdenPage.about_us'));

  describe('AboutUsCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var aboutCtrl = $controller('AboutUsCtrl');
      expect(aboutCtrl).toBeDefined();
    }));

  });
});
