'use strict';

describe('irdenPage.confirmation module', function() {

  beforeEach(module('irdenPage.confirmation'));

  describe('confirmation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var confirmationCtrl = $controller('ConfirmationCtrl');
      expect(confirmationCtrl).toBeDefined();
    }));

  });
});
