'use strict';

describe('irdenPage.feedback module', function() {

  beforeEach(module('irdenPage.feedback'));

  describe('feedback controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var feedbackCtrl = $controller('FeedbackCtrl');
      expect(feedbackCtrl).toBeDefined();
    }));

  });
});
