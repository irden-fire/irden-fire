'use strict';

describe('irdenPage.final module', function() {

  beforeEach(module('irdenPage.final'));

  describe('final controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var finalCtrl = $controller('FinalCtrl');
      expect(finalCtrl).toBeDefined();
    }));

  });
});
